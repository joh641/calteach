#container
  .content
    = render partial: 'left_column'
    .right-column-container.column
      %h2#name Add Item
      = render partial: 'item_form'
      %h2#name Add Multiple Items
      = form_tag import_items_path, :multipart => true do
        = file_field_tag :file
        = submit_tag "Import"
